{
  "tasks": {
    "dev": "deno run -A src/main.ts",
    "build": "deno bundle src/main.ts main.js && mkdir -p dist && cp index.html dist/ && cp main.js dist/main.js",
    "test": "deno test -A"
  }
}
